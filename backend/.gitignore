node_modules/
.env
.env.example
.env.*.local
# --- UPLOADS ---
# 1. On ignore TOUT ce qu'il y a dans uploads
uploads/*

# 2. Sauf le fichier .gitkeep (pour garder le dossier vide sur Git)
!uploads/.gitkeep